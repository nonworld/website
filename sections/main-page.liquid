<div class="non-section">
  <div class="non-section__head">
    <h1 class="non-section__title">{{ page.title }}</h1>
  </div>

  <div class="non-rte" style="margin-top:30px">
    {{ page.content }}
  </div>
</div>

{% schema %}
{ "name": "Page" }
{% endschema %}
