{%- comment -%}probe{%- endcomment -%}
<div class="non-process-anim">{% raw %}<x-dc><span style="color:{{ accent }}">{{ stepLabel }}</span></x-dc>{% endraw %}</div>

{% schema %}
{
  "name": "Process animation",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "How it's made" }
  ],
  "presets": [ { "name": "Process animation" } ]
}
{% endschema %}
