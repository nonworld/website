{
  "name": "non-lotto",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NON Lotto reveal API — Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/pool.test.js"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
